spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/gagagashop;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#          show_sql: true
        format_sql: true
        use_sql_comments: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: debug

file.dir: C:/Users/Administrator/study/file/